
@model IEnumerable <ShopFruistConnection.LOAISANPHAM>
@{
    Layout = null;
}

<ul class="sub-menu">
    @foreach (var item in Model)
    {
        <li><a href="@Url.Action("Index", "SanPham", new { id = item.MaLoaiSanPham })">@item.TenLoaiSanPham</a></li>
    }
</ul>
